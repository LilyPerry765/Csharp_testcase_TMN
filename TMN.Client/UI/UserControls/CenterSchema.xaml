<UserControl x:Class="TMN.UserControls.CenterSchema"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <!--<UserControl.Background>
        <LinearGradientBrush>
            <GradientStop Color="White"
                          Offset="0" />
            <GradientStop Color="LightBlue"
                          Offset="2" />
        </LinearGradientBrush>
    </UserControl.Background>-->
    <ScrollViewer x:Name="Root"
                  CanContentScroll="False"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  MouseMove="Root_MouseMove"
                  MouseUp="Root_MouseUp">
        <Label Opacity=".8"  x:Name="lblSelectCenter"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Foreground="White">
            <Label.Effect>
                <DropShadowEffect Direction="225"
                                  ShadowDepth="1" />
            </Label.Effect> لطفا يكي از مراكز سوييچ را انتخاب كنيد</Label>
    </ScrollViewer>
</UserControl>
